apiVersion: v1
kind: Namespace
metadata:
  name: crestmap
---
apiVersion: v1
kind: Secret
metadata:
  name: crestmap-db
  namespace: crestmap
type: Opaque
stringData:
  POSTGRES_DB: chp_map
  POSTGRES_USER: chp_map
  POSTGRES_PASSWORD: change-me
  DATABASE_URL: postgresql://chp_map:change-me@crestmap-postgres:5432/chp_map
  ADMIN_USERNAME: admin
  ADMIN_PASSWORD: change-me
  ADMIN_SESSION_SECRET: change-me-too
  OPENSKY_CLIENT_ID: ""
  OPENSKY_CLIENT_SECRET: ""
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crestmap-postgres-data
  namespace: crestmap
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: crestmap-postgres
  namespace: crestmap
spec:
  serviceName: crestmap-postgres
  replicas: 1
  selector:
    matchLabels:
      app: crestmap-postgres
  template:
    metadata:
      labels:
        app: crestmap-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - chp_map
                - -d
                - chp_map
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - chp_map
                - -d
                - chp_map
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: crestmap-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: crestmap-postgres
  namespace: crestmap
spec:
  type: ClusterIP
  selector:
    app: crestmap-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crestmap-scrape
  namespace: crestmap
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: crestmap-scrape
  template:
    metadata:
      labels:
        app: crestmap-scrape
    spec:
      containers:
        - name: scrape
          image: cajaks2/crestmap:0.1.298
          imagePullPolicy: IfNotPresent
          command:
            - python3
            - /app/scrape_chp_traffic.py
            - --interval
            - "60"
            - --detail-refresh-minutes
            - "3"
          ports:
            - name: metrics
              containerPort: 8081
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: DATABASE_URL
            - name: SERVICE_VERSION
              value: "0.1.298"
            - name: CHP_USER_AGENT
              value: "crestmap/0.1 (+https://crestmap.us/)"
            - name: CHP_SOURCE_MODE
              value: xml
            - name: CHP_XML_MAX_AGE_MINUTES
              value: "5"
            - name: CHP_XML_SHADOW_COMPARE
              value: "false"
            - name: SCRAPER_METRICS_HOST
              value: 0.0.0.0
            - name: SCRAPER_METRICS_PORT
              value: "8081"
            - name: PUBLIC_URL
              value: https://crestmap.us/
            - name: VAPID_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: VAPID_PRIVATE_KEY
                  optional: true
            - name: VAPID_SUBJECT
              value: https://crestmap.us
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: crestmap-scrape
  namespace: crestmap
spec:
  type: ClusterIP
  selector:
    app: crestmap-scrape
  ports:
    - name: metrics
      port: 8081
      targetPort: metrics
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crestmap-wildweb
  namespace: crestmap
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: crestmap-wildweb
  template:
    metadata:
      labels:
        app: crestmap-wildweb
    spec:
      containers:
        - name: wildweb
          image: cajaks2/crestmap:0.1.298
          imagePullPolicy: IfNotPresent
          command:
            - python3
            - /app/scrape_wildweb_incidents.py
          ports:
            - name: metrics
              containerPort: 8082
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: DATABASE_URL
            - name: SERVICE_VERSION
              value: "0.1.298"
            - name: WILDWEB_INTERVAL_SECONDS
              value: "120"
            - name: WILDWEB_MAX_AGE_HOURS
              value: "72"
            - name: WILDWEB_USER_AGENT
              value: "crestmap-wildweb/0.1 (+https://crestmap.us/)"
            - name: WILDWEB_PUSH_NOTIFICATIONS
              value: "true"
            - name: WILDWEB_METRICS_HOST
              value: 0.0.0.0
            - name: WILDWEB_METRICS_PORT
              value: "8082"
            - name: PUBLIC_URL
              value: https://crestmap.us/
            - name: VAPID_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: VAPID_PRIVATE_KEY
                  optional: true
            - name: VAPID_SUBJECT
              value: https://crestmap.us
          resources:
            requests:
              cpu: 10m
              memory: 48Mi
            limits:
              cpu: 100m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: crestmap-wildweb
  namespace: crestmap
spec:
  type: ClusterIP
  selector:
    app: crestmap-wildweb
  ports:
    - name: metrics
      port: 8082
      targetPort: metrics
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crestmap-aircraft
  namespace: crestmap
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: crestmap-aircraft
  template:
    metadata:
      labels:
        app: crestmap-aircraft
    spec:
      containers:
        - name: aircraft
          image: cajaks2/crestmap:0.1.298
          imagePullPolicy: IfNotPresent
          command:
            - python3
            - /app/aircraft_tracking.py
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: DATABASE_URL
            - name: SERVICE_VERSION
              value: "0.1.298"
            - name: OPENSKY_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: OPENSKY_CLIENT_ID
            - name: OPENSKY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: OPENSKY_CLIENT_SECRET
            - name: AIRCRAFT_POLL_SECONDS
              value: "30"
            - name: AIRCRAFT_RETENTION_HOURS
              value: "24"
          resources:
            requests:
              cpu: 10m
              memory: 48Mi
            limits:
              cpu: 100m
              memory: 128Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crestmap-web
  namespace: crestmap
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crestmap-web
  template:
    metadata:
      labels:
        app: crestmap-web
    spec:
      containers:
        - name: web
          image: cajaks2/crestmap:0.1.298
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - exec gunicorn app:app -k uvicorn.workers.UvicornWorker --workers ${WEB_WORKERS:-1} --bind 0.0.0.0:${HTTP_PORT:-8080} --access-logfile /dev/null --error-logfile -
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: DATABASE_URL
            - name: MAP_HOURS
              value: "72"
            - name: HTTP_PORT
              value: "8080"
            - name: SERVICE_VERSION
              value: "0.1.298"
            - name: BASE_PATH
              value: /
            - name: PUBLIC_URL
              value: https://crestmap.us/
            - name: GOOGLE_ANALYTICS_ID
              value: ""
            - name: VAPID_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: VAPID_PUBLIC_KEY
                  optional: true
            - name: AIRCRAFT_TRACKING_ENABLED
              value: "true"
            - name: AIRCRAFT_DISPLAY_DELAY_SECONDS
              value: "60"
            - name: AIRCRAFT_MAX_AGE_SECONDS
              value: "900"
            - name: AIRCRAFT_TRAIL_AGE_SECONDS
              value: "1800"
            - name: WEB_WORKERS
              value: "1"
            - name: COMMENT_AUTO_APPROVE
              value: "true"
            - name: ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: ADMIN_USERNAME
                  optional: true
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: ADMIN_PASSWORD
                  optional: true
            - name: R2_ACCOUNT_ID
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: R2_ACCOUNT_ID
                  optional: true
            - name: R2_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: R2_ACCESS_KEY_ID
                  optional: true
            - name: R2_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: R2_SECRET_ACCESS_KEY
                  optional: true
            - name: R2_UPLOAD_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: R2_UPLOAD_TOKEN_SECRET
                  optional: true
            - name: R2_BUCKET
              value: crestmap-media
            - name: ADMIN_SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: crestmap-db
                  key: ADMIN_SESSION_SECRET
                  optional: true
            - name: ADMIN_SESSION_HOURS
              value: "8"
            - name: ADMIN_SESSION_MAX_HOURS
              value: "24"
            - name: ADMIN_REMEMBER_DAYS
              value: "30"
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: crestmap
  namespace: crestmap
spec:
  type: ClusterIP
  selector:
    app: crestmap-web
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crestmap
  namespace: crestmap
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.middlewares: default-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: crestmap.us
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: crestmap
                port:
                  number: 80
  tls:
    - hosts:
        - crestmap.us
      secretName: crestmap-us-tls
